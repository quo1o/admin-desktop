import Terminal from './terminal';
import Printer from './printer';
import WSClient from './ws-client';

declare namespace PPS {
  export {
    Terminal,
    Printer,
    WSClient,
  };
}

export = PPS;
